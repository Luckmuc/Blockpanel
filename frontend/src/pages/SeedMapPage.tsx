import ServerConsolePage from "./ServerConsolePage";

// Backwards compatibility: SeedMapPage now re-exports ServerConsolePage
// This file intentionally contains only a re-export to avoid stale/duplicate
// map implementation remaining in the build.
export default ServerConsolePage;

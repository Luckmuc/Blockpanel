// API base URL for backend requests
// Use relative path so frontend always calls the same host as the website is served from
export const API_BASE = "/api"; // prefix all backend API calls with /api for NGINX proxy
